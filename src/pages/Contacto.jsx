import SectionTitle from "../Components/SectionTitle";

export default function Contacto() {
  return (
    <section className="p-8">
      <SectionTitle>Contacto</SectionTitle>
      <p>Redes sociales, GitHub, objetivo profesional.</p>
    </section>
  );
}